<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.haven.haven">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/BlueGreen">
        <activity
            android:name=".Controller.activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controller.activities.RegisterActivity"
            android:label="Register" />
        <activity
            android:name=".Controller.activities.MainPageActivity"
            android:label="@string/title_activity_side_bar"
            android:theme="@style/BlueGreen.NoActionBar" />
        <activity
            android:name=".Controller.activities.CreateNewShelterActivity"
            android:label="@string/title_activity_create_new_shelter"
            android:theme="@style/BlueGreen.NoActionBar" />
        <activity
            android:name=".Controller.activities.ShelterDetailsActivity"
            android:label="@string/title_activity_shelter_details"
            android:theme="@style/BlueGreen.NoActionBar" />
        <activity
            android:name=".Controller.activities.SearchedSheltersActivity"
            android:label="@string/title_activity_searched_shelters"
            android:theme="@style/BlueGreen.NoActionBar" />
        <activity
            android:name=".Controller.activities.ShelterListDetailsActivity"
            android:label="@string/title_activity_shelter_list_details"
            android:theme="@style/BlueGreen.NoActionBar"></activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBIKVhjjakdi59ecWHXAB4B-y8o9yXOIaY"/>
    </application>

</manifest>